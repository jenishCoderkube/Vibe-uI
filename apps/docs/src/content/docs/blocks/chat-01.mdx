# Chat 01

A conversational AI assistant and team messaging interface block featuring streaming message bubbles, syntax-highlighted code blocks, prompt history sidebars, and audio transcriptions.

## Preview

<ComponentPreview>
  <Chat01Demo />

```tsx
import Chat01Page from '@/components/blocks/chat-01/page'

export default function Chat01Demo() {
  return <Chat01Page />
}
```

</ComponentPreview>

<Installation name="chat-01" vibeDeps="avatar, button, card, dropdown-menu, input, scroll-area, separator, sheet, textarea, tooltip">

```bash
npx vibe-ui-kit add chat-01
```

</Installation>

## Block Concept

Vibe UI's Chat 01 block implements an AI-ready conversational workspace with auto-scrolling message streams, model selector menus, copyable code blocks, and conversation history drawers.
