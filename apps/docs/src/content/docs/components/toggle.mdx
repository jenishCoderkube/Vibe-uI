# Toggle

A two-state button component that can be pressed or active, designed to capture boolean actions.

## Preview: Toggle Heart Active

<ComponentPreview>
  <ToggleLike />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Heart } from 'lucide-react'

export default function HeartLikeDemo() {
  const [pressed, setPressed] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Active Like Option</h4>
        <p className="text-[10px] text-muted-foreground">Press to change liked heart status</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={pressed} onPressedChange={setPressed} variant="glow">
          <Heart
            className={`h-4 w-4 transition-all ${pressed ? 'fill-primary text-primary scale-110' : 'text-zinc-400'}`}
          />
          <span className="ml-2 text-xs">{pressed ? 'Liked' : 'Like'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

<Installation name="toggle" npmDeps="@radix-ui/react-toggle lucide-react tailwind-variants">

```tsx
"use client"

import * as React from "react"
import { tv, type VariantProps } from "tailwind-variants"
import { cn } from "@/lib/utils"

const toggleVariants = tv({
  base: "inline-flex items-center justify-center gap-2 rounded-md text-sm font-medium transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 select-none cursor-pointer border",
  variants: {
    variant: {
      default: "bg-transparent text-foreground border-transparent hover:bg-muted hover:text-muted-foreground data-[state=on]:bg-accent data-[state=on]:text-accent-foreground",
      glass: "bg-white/5 border-white/10 backdrop-blur-md text-zinc-400 hover:text-white hover:bg-white/10 data-[state=on]:bg-white/20 data-[state=on]:text-white data-[state=on]:border-white/30",
      retro: "border-2 border-foreground bg-background text-foreground shadow-[2px_2px_0px_0px_rgba(0,0,0,1)] dark:shadow-[2px_2px_0px_0px_rgba(255,255,255,1)] hover:bg-muted hover:text-foreground data-[state=on]:translate-x-[1px] data-[state=on]:translate-y-[1px] data-[state=on]:shadow-[1px_1px_0px_0px_rgba(0,0,0,1)] dark:data-[state=on]:shadow-[1px_1px_0px_0px_rgba(255,255,255,1)] data-[state=on]:bg-foreground data-[state=on]:text-background",
      glow: "border-primary/20 bg-primary/[0.02] text-primary/70 hover:text-primary hover:bg-primary/5 data-[state=on]:bg-primary/20 data-[state=on]:text-primary data-[state=on]:shadow-[0_0_15px_rgba(168,85,247,0.25)]",
      cyberpunk: "border-emerald-950 bg-black text-emerald-600 font-mono hover:text-emerald-400 hover:border-emerald-500/50 data-[state=on]:border-emerald-400 data-[state=on]:text-emerald-400 data-[state=on]:bg-emerald-500/10 data-[state=on]:shadow-[0_0_10px_rgba(16,185,129,0.2)]",
    },
    size: {
      default: "h-9 px-3 min-w-[36px]",
      sm: "h-8 px-2 text-xs min-w-[32px]",
      lg: "h-10 px-4 min-w-[40px]",
    },
  },
  defaultVariants: {
    variant: "default",
    size: "default",
  },
})

export interface ToggleProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof toggleVariants> {
  pressed?: boolean
  defaultPressed?: boolean
  onPressedChange?: (pressed: boolean) => void
}

const Toggle = React.forwardRef<HTMLButtonElement, ToggleProps>(
  ({ className, variant, size, pressed: pressedProp, defaultPressed = false, onPressedChange, onClick, ...props }, ref) => {
    const [pressedState, setPressedState] = React.useState(defaultPressed)
    const isControlled = pressedProp !== undefined
    const pressed = isControlled ? pressedProp : pressedState

    const handlePress = React.useCallback(
      (e: React.MouseEvent<HTMLButtonElement>) => {
        if (!isControlled) {
          setPressedState(!pressed)
        }
        onPressedChange?.(!pressed)
        onClick?.(e)
      },
      [isControlled, pressed, onPressedChange, onClick]
    )

    return (
      <button
        ref={ref}
        type="button"
        data-state={pressed ? "on" : "off"}
        aria-pressed={pressed}
        className={cn(toggleVariants({ variant, size }), className)}
        onClick={handlePress}
        {...props}
      />
    )
  }
)
Toggle.displayName = "Toggle"

export { Toggle, toggleVariants }
```

</Installation>



## Icon-Only Toggles (Showcase)

Compare 8 distinct interactive icon-only toggles (like Heart, Bookmark, Mute, Pin, Star, Visibility, Lock, Power) rendered inline:

<ComponentPreview>
  <ToggleIconShowcase />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Heart, Bookmark, Volume2, VolumeX, Pin, Star, Eye, EyeOff, Lock, Unlock, Power } from 'lucide-react'

export default function IconToggleGrid() {
  const [liked, setLiked] = useState(false)
  const [saved, setSaved] = useState(false)
  const [muted, setMuted] = useState(false)
  const [pinned, setPinned] = useState(false)
  const [starred, setStarred] = useState(false)
  const [visible, setVisible] = useState(true)
  const [locked, setLocked] = useState(false)
  const [active, setActive] = useState(false)

  return (
    <div className="w-full flex flex-wrap gap-4 items-center justify-center p-8 bg-zinc-950/20 rounded-xl border border-white/5">
      {/* 1. Heart Only */}
      <Toggle
        pressed={liked}
        onPressedChange={setLiked}
        variant="glow"
        size="lg"
        className="rounded-full h-11 w-11 p-0 flex items-center justify-center"
      >
        <Heart
          className={`h-5 w-5 transition-transform duration-200 ${liked ? 'fill-red-500 text-red-500 scale-110' : 'text-zinc-400'}`}
        />
      </Toggle>

      {/* 2. Save/Bookmark Only */}
      <Toggle
        pressed={saved}
        onPressedChange={setSaved}
        variant="glass"
        size="lg"
        className="h-11 w-11 p-0 flex items-center justify-center"
      >
        <Bookmark
          className={`h-5 w-5 transition-transform duration-200 ${saved ? 'fill-primary text-primary scale-110' : 'text-zinc-400'}`}
        />
      </Toggle>

      {/* 3. Mute Only */}
      <Toggle
        pressed={muted}
        onPressedChange={setMuted}
        variant="glow"
        size="lg"
        className="h-11 w-11 p-0 flex items-center justify-center"
      >
        {muted ? (
          <VolumeX className="h-5 w-5 text-red-400" />
        ) : (
          <Volume2 className="h-5 w-5 text-zinc-400" />
        )}
      </Toggle>

      {/* 4. Pin Only */}
      <Toggle
        pressed={pinned}
        onPressedChange={setPinned}
        variant="retro"
        size="lg"
        className="h-11 w-11 p-0 flex items-center justify-center"
      >
        <Pin
          className={`h-5 w-5 transition-transform duration-200 ${pinned ? 'rotate-45' : 'text-muted-foreground/60'}`}
        />
      </Toggle>

      {/* 5. Star Only */}
      <Toggle
        pressed={starred}
        onPressedChange={setStarred}
        variant="glow"
        size="lg"
        className="h-11 w-11 p-0 flex items-center justify-center"
      >
        <Star
          className={`h-5 w-5 transition-transform duration-200 ${starred ? 'fill-amber-400 text-amber-400 scale-110' : 'text-zinc-400'}`}
        />
      </Toggle>

      {/* 6. Eye Visibility Only */}
      <Toggle
        pressed={visible}
        onPressedChange={setVisible}
        variant="glass"
        size="lg"
        className="h-11 w-11 p-0 flex items-center justify-center"
      >
        {visible ? (
          <Eye className="h-5 w-5 text-emerald-400" />
        ) : (
          <EyeOff className="h-5 w-5 text-zinc-500" />
        )}
      </Toggle>

      {/* 7. Lock Only */}
      <Toggle
        pressed={locked}
        onPressedChange={setLocked}
        variant="cyberpunk"
        size="lg"
        className="h-11 w-11 p-0 flex items-center justify-center"
      >
        {locked ? (
          <Lock className="h-5 w-5 text-emerald-400" />
        ) : (
          <Unlock className="h-5 w-5 text-zinc-500" />
        )}
      </Toggle>

      {/* 8. Power Status */}
      <Toggle
        pressed={active}
        onPressedChange={setActive}
        variant="retro"
        size="lg"
        className="rounded-full h-11 w-11 p-0 flex items-center justify-center"
      >
        <Power
          className={`h-5 w-5 transition-transform duration-200 ${active ? 'text-emerald-500 scale-110' : 'text-muted-foreground'}`}
        />
      </Toggle>
    </div>
  )
}
```

</ComponentPreview>



## Standard Variations

### Theme Presets Comparison

<ComponentPreview>
  <ToggleVariantsDemo />

```tsx
import { Toggle } from '@/components/ui/toggle'
import { Bold, Italic } from 'lucide-react'

export default function PresetShowcase() {
  return (
    <div className="w-full flex flex-wrap gap-4 items-center justify-center p-8 bg-zinc-950/20 rounded-xl border border-white/5">
      <Toggle variant="default">
        <Bold className="h-4 w-4" />
      </Toggle>
      <Toggle variant="glass">
        <Italic className="h-4 w-4" />
      </Toggle>
      <Toggle variant="retro">
        <Bold className="h-4 w-4" />
      </Toggle>
      <Toggle variant="glow">
        <Italic className="h-4 w-4" />
      </Toggle>
      <Toggle variant="cyberpunk">
        <span className="font-mono text-xs">EXE</span>
      </Toggle>
    </div>
  )
}
```

</ComponentPreview>

### Format editor toolbar

<ComponentPreview>
  <ToggleToolbar />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Bold, Italic, Code } from 'lucide-react'

export default function ToolbarDemo() {
  const [bold, setBold] = useState(false)
  const [italic, setItalic] = useState(false)
  const [code, setCode] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Text Formatting</h4>
        <p className="text-[10px] text-muted-foreground">Toolbar select items matching actions</p>
      </div>
      <div className="flex gap-2 p-3 bg-zinc-900/40 rounded-lg justify-center">
        <Toggle pressed={bold} onPressedChange={setBold} variant="glass" size="sm">
          <Bold className="h-4 w-4" />
        </Toggle>
        <Toggle pressed={italic} onPressedChange={setItalic} variant="glass" size="sm">
          <Italic className="h-4 w-4" />
        </Toggle>
        <Toggle pressed={code} onPressedChange={setCode} variant="glass" size="sm">
          <Code className="h-4 w-4" />
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Audio volume controller

<ComponentPreview>
  <ToggleMute />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Volume2, VolumeX } from 'lucide-react'

export default function SoundDemo() {
  const [muted, setMuted] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Sound Control</h4>
        <p className="text-[10px] text-muted-foreground">Mute and unmute audio feeds</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={muted} onPressedChange={setMuted} variant="glow">
          {muted ? (
            <VolumeX className="h-4 w-4 text-primary" />
          ) : (
            <Volume2 className="h-4 w-4 text-zinc-400" />
          )}
          <span className="ml-2 text-xs">{muted ? 'Muted' : 'Playing'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Document saver bookmark

<ComponentPreview>
  <ToggleBookmark />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Bookmark } from 'lucide-react'

export default function BookmarkDemo() {
  const [saved, setSaved] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Save Progress</h4>
        <p className="text-[10px] text-muted-foreground">Bookmark directory logs</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={saved} onPressedChange={setSaved} variant="glass">
          <Bookmark className={`h-4 w-4 ${saved ? 'fill-white text-white' : 'text-zinc-400'}`} />
          <span className="ml-2 text-xs">{saved ? 'Saved' : 'Save Item'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Active pinned folder

<ComponentPreview>
  <TogglePin />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Pin } from 'lucide-react'

export default function PinDemo() {
  const [pinned, setPinned] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Pin Topic</h4>
        <p className="text-[10px] text-muted-foreground">Lock thread panel item to the top</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={pinned} onPressedChange={setPinned} variant="glow">
          <Pin className={`h-4 w-4 ${pinned ? 'rotate-45 text-primary' : 'text-zinc-400'}`} />
          <span className="ml-2 text-xs">{pinned ? 'Pinned' : 'Pin Thread'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Cyber terminal override

<ComponentPreview>
  <ToggleCyberGlitch />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'

export default function OverrideDemo() {
  const [glitch, setGlitch] = useState(true)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Cyberpunk Presets</h4>
        <p className="text-[10px] text-muted-foreground">Glitch scanline mode selector</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={glitch} onPressedChange={setGlitch} variant="cyberpunk">
          <span className="font-mono text-xs">{glitch ? 'GLITCH: ON' : 'GLITCH: OFF'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Grid vs List swapper

<ComponentPreview>
  <ToggleGridList />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { LayoutGrid, List } from 'lucide-react'

export default function SwapperDemo() {
  const [isGrid, setIsGrid] = useState(true)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Grid vs List</h4>
        <p className="text-[10px] text-muted-foreground">Control product display patterns</p>
      </div>
      <div className="flex gap-2 justify-center p-3 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={isGrid} onPressedChange={() => setIsGrid(true)} variant="glass">
          <LayoutGrid className="h-4 w-4" />
        </Toggle>
        <Toggle pressed={!isGrid} onPressedChange={() => setIsGrid(false)} variant="glass">
          <List className="h-4 w-4" />
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Retro Neobrutalist active options

<ComponentPreview>
  <ToggleBrutalistGrid />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'

export default function RetroOptionDemo() {
  const [active, setActive] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Retro Options</h4>
        <p className="text-[10px] text-muted-foreground">Thick outline flat option panel</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={active} onPressedChange={setActive} variant="retro">
          <span className="font-bold text-xs uppercase">Choose Variant</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Favorite star selector

<ComponentPreview>
  <ToggleStar />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { Star } from 'lucide-react'

export default function StarDemo() {
  const [fav, setFav] = useState(false)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Star Favorite</h4>
        <p className="text-[10px] text-muted-foreground">Mark components directory as starred</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={fav} onPressedChange={setFav} variant="glow">
          <Star className={`h-4 w-4 ${fav ? 'fill-amber-400 text-amber-400' : 'text-zinc-400'}`} />
          <span className="ml-2 text-xs">{fav ? 'Starred' : 'Favorite'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>

### Availability profile toggle

<ComponentPreview>
  <ToggleActiveUser />

```tsx
import React, { useState } from 'react'
import { Toggle } from '@/components/ui/toggle'
import { User } from 'lucide-react'

export default function UserStatusDemo() {
  const [online, setOnline] = useState(true)

  return (
    <div className="w-full max-w-[350px] overflow-hidden rounded-xl border border-white/10 bg-zinc-950/40 p-4 shadow-xl text-left text-white select-none">
      <div className="mb-3 space-y-0.5">
        <h4 className="text-xs font-bold uppercase tracking-wider">Developer Status</h4>
        <p className="text-[10px] text-muted-foreground">Show active availability settings</p>
      </div>
      <div className="flex justify-center p-4 bg-zinc-900/40 rounded-lg">
        <Toggle pressed={online} onPressedChange={setOnline} variant="glass">
          <User className="h-4 w-4" />
          <span className="ml-2 text-xs">{online ? 'Online' : 'Away'}</span>
        </Toggle>
      </div>
    </div>
  )
}
```

</ComponentPreview>
