# Slider

An input component that allows users to select a single value or range of values from a scale.

## Preview

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="50" max="100" step="1" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export function SliderDemo() {
  return <Slider defaultValue={[50]} max={100} step={1} className="w-[60%]" />
}
```

</ComponentPreview>

<Installation name="slider" npmDeps="@radix-ui/react-slider lucide-react tailwind-variants">

```tsx
// @/components/ui/slider.tsx
"use client"

import * as React from 'react'
import * as SliderPrimitive from '@radix-ui/react-slider'
import { tv } from 'tailwind-variants'
import { cn } from '@/lib/utils'

const sliderRootVariants = tv({
  base: 'relative flex touch-none select-none items-center data-[orientation=horizontal]:w-full data-[orientation=horizontal]:min-w-[200px] data-[orientation=horizontal]:h-5 data-[orientation=vertical]:h-full data-[orientation=vertical]:min-h-[150px] data-[orientation=vertical]:w-5 data-[orientation=vertical]:flex-col',
})

const sliderTrackVariants = tv({
  base: 'relative grow overflow-hidden rounded-full data-[orientation=horizontal]:w-full data-[orientation=vertical]:h-full data-[orientation=horizontal]:h-2 data-[orientation=vertical]:w-2',
  variants: {
    variant: {
      default: 'bg-secondary/50 dark:bg-zinc-800/80 border border-border/50',
      retro: 'border-2 border-foreground bg-background data-[orientation=horizontal]:h-3 data-[orientation=vertical]:w-3 rounded-none',
      glass: 'bg-white/10 dark:bg-black/40 border border-white/20 dark:border-white/10 backdrop-blur-md shadow-inner',
    }
  },
  defaultVariants: {
    variant: 'default',
  }
})

const sliderRangeVariants = tv({
  base: 'absolute rounded-full data-[orientation=horizontal]:h-full data-[orientation=vertical]:w-full',
  variants: {
    variant: {
      default: 'bg-primary dark:bg-white',
      retro: 'bg-foreground rounded-none',
      glass: 'bg-gradient-to-r from-violet-500 to-indigo-500',
    }
  },
  defaultVariants: {
    variant: 'default',
  }
})

const sliderThumbVariants = tv({
  base: 'relative block rounded-full bg-background shadow-md transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 cursor-grab active:cursor-grabbing',
  variants: {
    variant: {
      default: 'h-5 w-5 border-2 border-primary dark:border-white',
      retro: 'h-6 w-6 border-2 border-foreground bg-background shadow-[2px_2px_0px_0px_rgba(0,0,0,1)] dark:shadow-[2px_2px_0px_0px_rgba(255,255,255,1)] rounded-none',
      glass: 'h-5 w-5 bg-white/30 dark:bg-black/50 backdrop-blur-md border border-white/50 dark:border-white/30 shadow-md',
    }
  },
  defaultVariants: {
    variant: 'default',
  }
})

export interface SliderProps extends Omit<React.ComponentPropsWithoutRef<typeof SliderPrimitive.Root>, 'value' | 'defaultValue'> {
  variant?: 'default' | 'retro' | 'glass'
  value?: number[]
  defaultValue?: number[]
  showTooltip?: boolean
  tooltipFormat?: (value: number) => string
  marks?: boolean | { value: number; label?: string }[]
}

const Slider = React.forwardRef<
  React.ElementRef<typeof SliderPrimitive.Root>,
  SliderProps
>(({ className, variant, value, defaultValue, min = 0, max = 100, step = 1, showTooltip = false, tooltipFormat, marks, ...props }, ref) => {
  const numMin = typeof min === 'string' ? parseFloat(min) : min
  const numMax = typeof max === 'string' ? parseFloat(max) : max
  const numStep = typeof step === 'string' ? parseFloat(step) : step

  const [localValue, setLocalValue] = React.useState<number[]>(() => {
    return value || defaultValue || [numMin]
  })

  React.useEffect(() => {
    if (value !== undefined) {
      setLocalValue(value)
    }
  }, [value])

  const handleValueChange = (val: number[]) => {
    setLocalValue(val)
    props.onValueChange?.(val)
  }

  let tickValues: { value: number; label?: string }[] = []
  if (marks === true && numStep) {
    for (let v = numMin; v <= numMax; v += numStep) {
      tickValues.push({ value: v })
    }
  } else if (Array.isArray(marks)) {
    tickValues = marks
  }

  return (
    <div className={cn('relative w-full', tickValues.some(t => t.label) && 'mb-8')}>
      <SliderPrimitive.Root
        ref={ref}
        className={cn(sliderRootVariants(), className)}
        value={value}
        defaultValue={defaultValue}
        min={numMin}
        max={numMax}
        step={numStep}
        onValueChange={handleValueChange}
        {...props}
      >
        <SliderPrimitive.Track className={cn(sliderTrackVariants({ variant }))}>
          <SliderPrimitive.Range className={cn(sliderRangeVariants({ variant }))} />
          {tickValues.map((tick, idx) => {
            const pct = ((tick.value - numMin) / (numMax - numMin)) * 100
            return (
              <span
                key={idx}
                className="absolute top-1/2 -translate-y-1/2 h-1.5 w-1.5 rounded-full bg-foreground/20 dark:bg-white/30 pointer-events-none"
                style={{ left: `${pct}%`, transform: 'translate(-50%, -50%)' }}
              />
            )
          })}
        </SliderPrimitive.Track>
        {localValue.map((val, index) => (
          <SliderPrimitive.Thumb
            key={index}
            className={cn(sliderThumbVariants({ variant }))}
          >
            {showTooltip && (
              <div className="absolute bottom-full left-1/2 -translate-x-1/2 mb-2.5 bg-zinc-800 text-white text-[10px] font-bold px-1.5 py-0.5 rounded shadow-sm whitespace-nowrap after:content-[''] after:absolute after:top-full after:left-1/2 after:-translate-x-1/2 after:border-4 after:border-transparent after:border-t-zinc-800 pointer-events-none select-none z-30">
                {tooltipFormat ? tooltipFormat(val) : val}
              </div>
            )}
          </SliderPrimitive.Thumb>
        ))}
      </SliderPrimitive.Root>
      {tickValues.map((tick, idx) => {
        if (!tick.label) return null
        const pct = ((tick.value - numMin) / (numMax - numMin)) * 100
        return (
          <span
            key={idx}
            className="absolute top-6 text-[10px] font-medium text-muted-foreground -translate-x-1/2 whitespace-nowrap pointer-events-none select-none"
            style={{ left: `${pct}%` }}
          >
            {tick.label}
          </span>
        )
      })}
    </div>
  )
})
Slider.displayName = SliderPrimitive.Root.displayName

export { Slider }
```

</Installation>


## All Variations

Explore 10 different variations and styling presets of the Slider component.

### 1. Default Style

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="25" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function DefaultSliderDemo() {
  return <Slider defaultValue={[25]} />
}
```

</ComponentPreview>

### 2. Range Double Handle

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="20,80" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function RangeSliderDemo() {
  return <Slider defaultValue={[20, 80]} />
}
```

</ComponentPreview>

### 3. Glassmorphic Style

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="60" variant="glass" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function GlassSliderDemo() {
  return <Slider defaultValue={[60]} variant="glass" />
}
```

</ComponentPreview>

### 4. Retro Neobrutalist Style

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="40" variant="retro" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function RetroSliderDemo() {
  return <Slider defaultValue={[40]} variant="retro" />
}
```

</ComponentPreview>

### 5. Stepped Slider Marks (Step=10)

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="50" step="10" max="100" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function SteppedSliderDemo() {
  return <Slider defaultValue={[50]} step={10} max={100} />
}
```

</ComponentPreview>

### 6. Disabled State Slider

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="30" disabled />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function DisabledSliderDemo() {
  return <Slider defaultValue={[30]} disabled />
}
```

</ComponentPreview>

### 7. Custom Styled Accent Track Fill

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="75" className="[&>span>span]:bg-emerald-500" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function AccentTrackSliderDemo() {
  return <Slider defaultValue={[75]} className="[&>span>span]:bg-emerald-500" />
}
```

</ComponentPreview>

### 8. Vertical Orientation

<ComponentPreview>
  <PreviewWrapper className="h-32 justify-center">
    <Slider defaultValue="50" orientation="vertical" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function VerticalSliderDemo() {
  return (
    <div className="h-32">
      <Slider defaultValue={[50]} orientation="vertical" className="h-full" />
    </div>
  )
}
```

</ComponentPreview>

### 9. Custom Large Handles

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue="50" className="[&>span:nth-child(2)]:h-7 [&>span:nth-child(2)]:w-7" />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function CustomThumbSliderDemo() {
  return <Slider defaultValue={[50]} className="[&>span:nth-child(2)]:h-7 [&>span:nth-child(2)]:w-7" />
}
```

</ComponentPreview>

### 10. Slider with Text Value Indicator

<ComponentPreview>
  <SliderValueDemo />

```tsx
import React, { useState } from 'react'
import { Slider } from '@/components/ui/slider'

export default function LabeledSliderDemo() {
  const [value, setValue] = useState([80])

  return (
    <div className="w-full flex flex-col gap-4">
      <div className="flex justify-between items-center text-xs font-semibold text-foreground">
        <span>Volume setting</span>
        <span className="font-mono bg-primary/10 text-primary px-2 py-0.5 rounded">
          {value[0]}%
        </span>
      </div>
      <Slider value={value} onValueChange={setValue} max={100} step={1} />
    </div>
  )
}
```

</ComponentPreview>

### 11. Continuous Value Tooltips

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue={[70]} showTooltip max={100} />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function TooltipSliderDemo() {
  return <Slider defaultValue={[70]} showTooltip max={100} />
}
```

</ComponentPreview>

### 12. Discrete Step Ticks

<ComponentPreview>
  <PreviewWrapper>
    <Slider defaultValue={[30]} step={10} max={100} marks={true} showTooltip />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function SteppedTicksSliderDemo() {
  return <Slider defaultValue={[30]} step={10} max={100} marks={true} showTooltip />
}
```

</ComponentPreview>

### 13. Rich Labels & Custom Marks

<ComponentPreview>
  <PreviewWrapper className="py-6">
    <Slider
      defaultValue={[20]}
      min={0}
      max={100}
      showTooltip
      marks={[
        { value: 0, label: '0°C' },
        { value: 20, label: '20°C' },
        { value: 37, label: '37°C' },
        { value: 100, label: '100°C' }
      ]}
    />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function LabeledMarksSliderDemo() {
  return (
    <Slider
      defaultValue={[20]}
      min={0}
      max={100}
      showTooltip
      marks={[
        { value: 0, label: '0°C' },
        { value: 20, label: '20°C' },
        { value: 37, label: '37°C' },
        { value: 100, label: '100°C' }
      ]}
    />
  )
}
```

</ComponentPreview>

### 14. Range Slider with Tooltips

<ComponentPreview>
  <PreviewWrapper className="py-6">
    <Slider defaultValue={[20, 80]} min={0} max={100} showTooltip />
  </PreviewWrapper>

```tsx
import { Slider } from '@/components/ui/slider'

export default function RangeTooltipSliderDemo() {
  return <Slider defaultValue={[20, 80]} min={0} max={100} showTooltip />
}
```

</ComponentPreview>

