# Pagination

A customizable component for page navigation controls.

## Preview

<ComponentPreview>
  <PaginationDemoBasic />

```tsx
import React from 'react'
import {
  Pagination,
  PaginationContent,
  PaginationEllipsis,
  PaginationItem,
  PaginationLink,
  PaginationNext,
  PaginationPrevious,
} from '@/components/ui/pagination'

// Helper function to calculate active pagination range with ellipsis
function getPaginationRange(current: number, total: number) {
  const range: (number | 'ellipsis')[] = []
  if (total <= 5) {
    for (let i = 1; i <= total; i++) range.push(i)
    return range
  }
  range.push(1)
  if (current > 3) range.push('ellipsis')
  const start = Math.max(2, current - 1)
  const end = Math.min(total - 1, current + 1)
  for (let i = start; i <= end; i++) {
    if (!range.includes(i)) range.push(i)
  }
  if (current < total - 2) range.push('ellipsis')
  if (!range.includes(total)) range.push(total)
  return range
}

export default function PaginationDemo() {
  const [page, setPage] = React.useState(1)
  const total = 10
  const range = getPaginationRange(page, total)

  return (
    <Pagination>
      <PaginationContent>
        <PaginationItem>
          <PaginationPrevious
            onClick={() => setPage((p) => Math.max(1, p - 1))}
            className="cursor-pointer"
          />
        </PaginationItem>
        {range.map((item, index) => {
          if (item === 'ellipsis') {
            return (
              <PaginationItem key={`ellipsis-${index}`}>
                <PaginationEllipsis />
              </PaginationItem>
            )
          }
          return (
            <PaginationItem key={item}>
              <PaginationLink
                isActive={page === item}
                onClick={() => setPage(item as number)}
                className="cursor-pointer"
              >
                {item}
              </PaginationLink>
            </PaginationItem>
          )
        })}
        <PaginationItem>
          <PaginationNext
            onClick={() => setPage((p) => Math.min(total, p + 1))}
            className="cursor-pointer"
          />
        </PaginationItem>
      </PaginationContent>
    </Pagination>
  )
}
```

</ComponentPreview>

<Installation name="pagination" vibeDeps="button">

```tsx
'use client'

import * as React from 'react'
import { ChevronLeft, ChevronRight, MoreHorizontal } from 'lucide-react'
import { tv, type VariantProps } from 'tailwind-variants'
import { cn } from '@/lib/utils'
import { buttonVariants } from './button'

const paginationVariants = tv({
  base: 'mx-auto flex w-full justify-center',
  variants: {
    variant: {
      default: '',
      glass: '',
      retro: '',
      glow: '',
    },
  },
  defaultVariants: {
    variant: 'default',
  },
})

export interface PaginationProps
  extends React.ComponentPropsWithoutRef<'nav'>,
    VariantProps<typeof paginationVariants> {}

const Pagination = React.forwardRef<HTMLElement, PaginationProps>(
  ({ className, ...props }, ref) => (
    <nav
      ref={ref}
      role="navigation"
      aria-label="pagination"
      className={cn('mx-auto flex w-full justify-center', className)}
      {...props}
    />
  )
)
Pagination.displayName = 'Pagination'

const PaginationContent = React.forwardRef<
  HTMLUListElement,
  React.ComponentPropsWithoutRef<'ul'>
>(({ className, ...props }, ref) => (
  <ul
    ref={ref}
    className={cn('flex flex-row items-center gap-1.5', className)}
    {...props}
  />
))
PaginationContent.displayName = 'PaginationContent'

const PaginationItem = React.forwardRef<
  HTMLLIElement,
  React.ComponentPropsWithoutRef<'li'>
>(({ className, ...props }, ref) => (
  <li ref={ref} className={cn('', className)} {...props} />
))
PaginationItem.displayName = 'PaginationItem'

type PaginationLinkProps = {
  isActive?: boolean
} & React.ComponentPropsWithoutRef<'a'> &
  VariantProps<typeof buttonVariants>

const PaginationLink = ({
  className,
  isActive,
  variant,
  size = 'icon',
  ...props
}: PaginationLinkProps) => (
  <a
    aria-current={isActive ? 'page' : undefined}
    className={cn(
      buttonVariants({
        variant: isActive ? (variant === 'retro' ? 'retro' : variant === 'glow' ? 'glow' : 'default') : 'outline',
        size,
      }),
      className
    )}
    {...props}
  />
)
PaginationLink.displayName = 'PaginationLink'

const PaginationPrevious = ({
  className,
  variant,
  ...props
}: React.ComponentProps<typeof PaginationLink>) => (
  <PaginationLink
    aria-label="Go to previous page"
    size="default"
    className={cn('gap-1 pl-2.5', className)}
    variant={variant}
    {...props}
  >
    <ChevronLeft className="h-4 w-4" />
    <span>Previous</span>
  </PaginationLink>
)
PaginationPrevious.displayName = 'PaginationPrevious'

const PaginationNext = ({
  className,
  variant,
  ...props
}: React.ComponentProps<typeof PaginationLink>) => (
  <PaginationLink
    aria-label="Go to next page"
    size="default"
    className={cn('gap-1 pr-2.5', className)}
    variant={variant}
    {...props}
  >
    <span>Next</span>
    <ChevronRight className="h-4 w-4" />
  </PaginationLink>
)
PaginationNext.displayName = 'PaginationNext'

const PaginationEllipsis = ({
  className,
  ...props
}: React.ComponentProps<'span'>) => (
  <span
    aria-hidden
    className={cn('flex h-9 w-9 items-center justify-center', className)}
    {...props}
  >
    <MoreHorizontal className="h-4 w-4" />
    <span className="sr-only">More pages</span>
  </span>
)
PaginationEllipsis.displayName = 'PaginationEllipsis'

export {
  type PaginationProps,
  Pagination,
  PaginationContent,
  PaginationEllipsis,
  PaginationItem,
  PaginationLink,
  PaginationNext,
  PaginationPrevious,
}
```

</Installation>



## Themes

Explore design variant presets for the Pagination component side-by-side.

<ComponentPreview>
  <div className="flex flex-col gap-6 w-full max-w-xl py-2 items-center">
    {/* Default Theme */}
    <div className="space-y-1.5 w-full flex flex-col items-center">
      <span className="text-xs font-semibold text-muted-foreground self-start">Default</span>
      <PaginationDemoBasic />
    </div>

    {/* Glassmorphism Theme */}
    <div className="space-y-1.5 w-full flex flex-col items-center">
      <span className="text-xs font-semibold text-muted-foreground self-start">Glassmorphism</span>
      <PaginationDemoGlass />
    </div>

    {/* Retro Brutalist Theme */}
    <div className="space-y-1.5 w-full flex flex-col items-center">
      <span className="text-xs font-semibold text-muted-foreground self-start">Brutalist Retro</span>
      <PaginationDemoRetro />
    </div>

    {/* Neon Glow Theme */}
    <div className="space-y-1.5 w-full flex flex-col items-center">
      <span className="text-xs font-semibold text-muted-foreground self-start">Neon Glow</span>
      <PaginationDemoGlow />
    </div>
  </div>

```tsx
import React, { useState } from 'react'
import {
  Pagination,
  PaginationContent,
  PaginationEllipsis,
  PaginationItem,
  PaginationLink,
  PaginationNext,
  PaginationPrevious,
} from '@/components/ui/pagination'

export default function PaginationThemesDemo(): React.ReactElement {
  const [page, setPage] = useState<number>(1)
  const total = 5

  return (
    <div className="flex flex-col gap-6 w-full max-w-xl">
      {/* Default Theme */}
      <div className="space-y-1.5">
        <span className="text-xs font-semibold text-muted-foreground">Default</span>
        <Pagination>
          <PaginationContent>
            <PaginationItem><PaginationPrevious onClick={() => setPage(p => Math.max(1, p - 1))} /></PaginationItem>
            <PaginationItem><PaginationLink isActive={page === 1} onClick={() => setPage(1)}>1</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink isActive={page === 2} onClick={() => setPage(2)}>2</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink isActive={page === 3} onClick={() => setPage(3)}>3</PaginationLink></PaginationItem>
            <PaginationItem><PaginationNext onClick={() => setPage(p => Math.min(total, p + 1))} /></PaginationItem>
          </PaginationContent>
        </Pagination>
      </div>

      {/* Glassmorphism Theme */}
      <div className="space-y-1.5">
        <span className="text-xs font-semibold text-muted-foreground">Glassmorphism</span>
        <Pagination>
          <PaginationContent>
            <PaginationItem><PaginationPrevious variant="glass" onClick={() => setPage(p => Math.max(1, p - 1))} /></PaginationItem>
            <PaginationItem><PaginationLink variant="glass" isActive={page === 1} onClick={() => setPage(1)}>1</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink variant="glass" isActive={page === 2} onClick={() => setPage(2)}>2</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink variant="glass" isActive={page === 3} onClick={() => setPage(3)}>3</PaginationLink></PaginationItem>
            <PaginationItem><PaginationNext variant="glass" onClick={() => setPage(p => Math.min(total, p + 1))} /></PaginationItem>
          </PaginationContent>
        </Pagination>
      </div>

      {/* Retro Brutalist Theme */}
      <div className="space-y-1.5">
        <span className="text-xs font-semibold text-muted-foreground">Brutalist Retro</span>
        <Pagination>
          <PaginationContent>
            <PaginationItem><PaginationPrevious variant="retro" onClick={() => setPage(p => Math.max(1, p - 1))} /></PaginationItem>
            <PaginationItem><PaginationLink variant="retro" isActive={page === 1} onClick={() => setPage(1)}>1</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink variant="retro" isActive={page === 2} onClick={() => setPage(2)}>2</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink variant="retro" isActive={page === 3} onClick={() => setPage(3)}>3</PaginationLink></PaginationItem>
            <PaginationItem><PaginationNext variant="retro" onClick={() => setPage(p => Math.min(total, p + 1))} /></PaginationItem>
          </PaginationContent>
        </Pagination>
      </div>

      {/* Neon Glow Theme */}
      <div className="space-y-1.5">
        <span className="text-xs font-semibold text-muted-foreground">Neon Glow</span>
        <Pagination>
          <PaginationContent>
            <PaginationItem><PaginationPrevious variant="glow" onClick={() => setPage(p => Math.max(1, p - 1))} /></PaginationItem>
            <PaginationItem><PaginationLink variant="glow" isActive={page === 1} onClick={() => setPage(1)}>1</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink variant="glow" isActive={page === 2} onClick={() => setPage(2)}>2</PaginationLink></PaginationItem>
            <PaginationItem><PaginationLink variant="glow" isActive={page === 3} onClick={() => setPage(3)}>3</PaginationLink></PaginationItem>
            <PaginationItem><PaginationNext variant="glow" onClick={() => setPage(p => Math.min(total, p + 1))} /></PaginationItem>
          </PaginationContent>
        </Pagination>
      </div>
    </div>
  )
}
```
</ComponentPreview>

---

## Examples

### 1. Advanced Navigation Controls

A combination of quick page jumper dropdowns and range slider navigation controls.

<ComponentPreview>
  <PaginationDemoNavControls />

```tsx
import React, { useState } from 'react'
import { PaginationDropdown, PaginationSlider } from '@/components/ui/pagination'

export default function NavigationControlsDemo() {
  const [page, setPage] = useState<number>(4)
  const totalPages = 10

  return (
    <div className="flex flex-col gap-4 w-full max-w-md items-center">
      <PaginationDropdown
        totalPages={totalPages}
        currentPage={page}
        onPageChange={setPage}
      />
      <PaginationSlider
        totalPages={totalPages}
        currentPage={page}
        onPageChange={setPage}
      />
    </div>
  )
}
```
</ComponentPreview>

### 2. Stateful Load More Progress

A progress bar showing loaded item stats with an interactive "Load More" action button and spinner animation.

<ComponentPreview>
  <PaginationDemoNewLoadMore />

```tsx
import React, { useState } from 'react'
import { PaginationLoadMore } from '@/components/ui/pagination'

export default function LoadMoreProgressDemo() {
  const [loaded, setLoaded] = useState<number>(25)
  const [loading, setLoading] = useState<boolean>(false)
  const total = 100

  const handleLoadMore = () => {
    setLoading(true)
    setTimeout(() => {
      setLoaded((prev) => Math.min(total, prev + 25))
      setLoading(false)
    }, 600)
  }

  return (
    <PaginationLoadMore
      isLoading={loading}
      onClick={handleLoadMore}
      hasNextPage={loaded < total}
      loadedCount={loaded}
      totalCount={total}
      variant="default"
    />
  )
}
```
</ComponentPreview>

## Props

### Pagination

<PropsTable>
  <PropRow
    prop="className"
    type="string"
    description="Additional utility styles for layout alignment."
  />
</PropsTable>

### PaginationLink

<PropsTable>
  <PropRow
    prop="isActive"
    type="boolean"
    default="false"
    description="When true, highlights the page button as the current active page."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'outline' | 'secondary' | 'ghost'"
    default="'outline'"
    description="The visual aesthetic theme applied to the pagination link control."
  />
  <PropRow
    prop="size"
    type="'default' | 'sm' | 'lg' | 'icon'"
    default="'icon'"
    description="Size variant preset for the button container."
  />
  <PropRow
    prop="asButton"
    type="boolean"
    default="false"
    description="When true, renders a native button element instead of an anchor link tag."
  />
</PropsTable>

### PaginationDots

<PropsTable>
  <PropRow
    prop="totalPages"
    type="number"
    description="Total number of pages to render indicator dots for."
  />
  <PropRow
    prop="currentPage"
    type="number"
    description="Currently active page index."
  />
  <PropRow
    prop="onPageChange"
    type="(page: number) => void"
    description="Callback function triggered when a dot is clicked."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'cyberpunk'"
    default="'default'"
    description="Visual style theme preset for the dot navigation bar."
  />
</PropsTable>

### PaginationSlider

<PropsTable>
  <PropRow
    prop="totalPages"
    type="number"
    description="Total number of pages available on the range slider."
  />
  <PropRow
    prop="currentPage"
    type="number"
    description="Current page value selected by the slider input."
  />
  <PropRow
    prop="onPageChange"
    type="(page: number) => void"
    description="Callback function triggered when the slider value changes."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'cyberpunk'"
    default="'default'"
    description="Visual theme preset applied to the range slider."
  />
</PropsTable>

### PaginationMini

<PropsTable>
  <PropRow
    prop="totalPages"
    type="number"
    description="Total number of pages."
  />
  <PropRow
    prop="currentPage"
    type="number"
    description="Current active page."
  />
  <PropRow
    prop="onPageChange"
    type="(page: number) => void"
    description="Callback function triggered when clicking Previous or Next."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'cyberpunk'"
    default="'default'"
    description="Visual theme preset for the compact mini pagination bar."
  />
</PropsTable>

### PaginationDropdown

<PropsTable>
  <PropRow
    prop="totalPages"
    type="number"
    description="Total number of pages available in the select dropdown."
  />
  <PropRow
    prop="currentPage"
    type="number"
    description="Currently selected page."
  />
  <PropRow
    prop="onPageChange"
    type="(page: number) => void"
    description="Callback function triggered when a page option is selected."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'cyberpunk'"
    default="'default'"
    description="Visual theme preset for the dropdown pagination component."
  />
</PropsTable>

### PaginationProgressLine

<PropsTable>
  <PropRow
    prop="totalPages"
    type="number"
    description="Total count of pages."
  />
  <PropRow
    prop="currentPage"
    type="number"
    description="Active page number."
  />
  <PropRow
    prop="onPageChange"
    type="(page: number) => void"
    description="Callback triggered when navigating pages."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'cyberpunk'"
    default="'default'"
    description="Visual theme preset for the progress line component."
  />
</PropsTable>

### PaginationLoadMore

<PropsTable>
  <PropRow
    prop="isLoading"
    type="boolean"
    description="Whether new items are currently being fetched (shows spinner animation)."
  />
  <PropRow
    prop="onClick"
    type="() => void"
    description="Callback function when user clicks Load More button."
  />
  <PropRow
    prop="hasNextPage"
    type="boolean"
    description="When true, displays the Load More action button."
  />
  <PropRow
    prop="loadedCount"
    type="number"
    description="Optional number of items loaded so far for progress line calculation."
  />
  <PropRow
    prop="totalCount"
    type="number"
    description="Optional total number of available items across all pages."
  />
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow' | 'cyberpunk'"
    default="'default'"
    description="Visual theme preset."
  />
</PropsTable>

