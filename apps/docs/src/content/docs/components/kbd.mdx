# Kbd

Displays a keyboard shortcut key indicator.

## Preview

<ComponentPreview name="kbd">
  <div className="flex items-center space-x-2 text-left">
    <span className="text-sm text-muted-foreground">Press</span>
    <Kbd>⌘</Kbd>
    <Kbd>K</Kbd>
    <span className="text-sm text-muted-foreground">to open search.</span>
  </div>

```tsx
import { Kbd } from '@/components/ui/kbd'

export default function KbdDemo() {
  return (
    <div className="flex items-center space-x-2">
      <span className="text-sm text-muted-foreground">Press</span>
      <Kbd>⌘</Kbd>
      <Kbd>K</Kbd>
      <span className="text-sm text-muted-foreground">to open search.</span>
    </div>
  )
}
```

</ComponentPreview>

<Installation name="kbd" vibeDeps="" npmDeps="tailwind-variants @radix-ui/react-slot">

```tsx
'use client'

import * as React from 'react'
import { Slot } from '@radix-ui/react-slot'
import { tv, type VariantProps } from 'tailwind-variants'
import { cn } from '@/lib/utils'

const kbdVariants = tv({
  base: 'pointer-events-none inline-flex h-5 select-none items-center gap-1 rounded border bg-muted px-1.5 font-mono text-[10px] font-medium text-muted-foreground opacity-100 shadow-sm transition-all duration-200',
  variants: {
    variant: {
      default: 'border-border',
      glass:
        'bg-black/5 dark:bg-white/5 border-black/10 dark:border-white/10 text-foreground backdrop-blur-md',
      retro:
        'border-2 border-foreground bg-background text-foreground shadow-[1px_1px_0px_0px_rgba(0,0,0,1)] dark:shadow-[1px_1px_0px_0px_rgba(255,255,255,1)] rounded-none',
      glow: 'border-border shadow-[0_0_10px_rgba(0,0,0,0.05)]',
    },
  },
  defaultVariants: {
    variant: 'default',
  },
})

export interface KbdProps
  extends React.HTMLAttributes<HTMLElement>, VariantProps<typeof kbdVariants> {
  asChild?: boolean
}

const Kbd = React.forwardRef<HTMLElement, KbdProps>(
  ({ className, variant = 'default', asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : 'kbd'
    return (
      <Comp
        ref={ref}
        data-slot="kbd"
        className={cn(kbdVariants({ variant }), className)}
        {...props}
      />
    )
  },
)
Kbd.displayName = 'Kbd'

export { Kbd }
```

</Installation>

## Examples

Explore layout variations and visual themes.

<ComponentPreview name="kbd">
  <div className="grid grid-cols-2 md:grid-cols-4 gap-8 py-6 w-full max-w-xl mx-auto text-left">
    
    {/* Default */}
    <div className="space-y-2">
      <div className="text-[10px] uppercase font-bold text-muted-foreground tracking-wider">Default</div>
      <div className="flex items-center gap-1">
        <Kbd variant="default">⌘</Kbd>
        <Kbd variant="default">K</Kbd>
      </div>
    </div>

    {/* Glassmorphic */}
    <div className="space-y-2">
      <div className="text-[10px] uppercase font-bold text-muted-foreground tracking-wider">Glassmorphic</div>
      <div className="flex items-center gap-1">
        <Kbd variant="glass">⌘</Kbd>
        <Kbd variant="glass">P</Kbd>
      </div>
    </div>

    {/* Retro Neobrutalism */}
    <div className="space-y-2">
      <div className="text-[10px] uppercase font-bold text-muted-foreground tracking-wider">Retro</div>
      <div className="flex items-center gap-1">
        <Kbd variant="retro">⌘</Kbd>
        <Kbd variant="retro">S</Kbd>
      </div>
    </div>

    {/* Glow shadow */}
    <div className="space-y-2">
      <div className="text-[10px] uppercase font-bold text-muted-foreground tracking-wider">Glow</div>
      <div className="flex items-center gap-1">
        <Kbd variant="glow">⌘</Kbd>
        <Kbd variant="glow">H</Kbd>
      </div>
    </div>

  </div>

```tsx
import { Kbd } from '@/components/ui/kbd'

export default function KbdVariants() {
  return (
    <div className="flex items-center space-x-6">
      <div className="flex items-center gap-1">
        <Kbd variant="default">⌘</Kbd>
        <Kbd variant="default">K</Kbd>
      </div>
      <div className="flex items-center gap-1">
        <Kbd variant="glass">⌘</Kbd>
        <Kbd variant="glass">P</Kbd>
      </div>
      <div className="flex items-center gap-1">
        <Kbd variant="retro">⌘</Kbd>
        <Kbd variant="retro">S</Kbd>
      </div>
      <div className="flex items-center gap-1">
        <Kbd variant="glow">⌘</Kbd>
        <Kbd variant="glow">H</Kbd>
      </div>
    </div>
  )
}
```

</ComponentPreview>

## Props

<PropsTable>
  <PropRow
    prop="variant"
    type="'default' | 'glass' | 'retro' | 'glow'"
    default="'default'"
    description="The visual aesthetic theme of the keycaps."
  />
  <PropRow
    prop="asChild"
    type="boolean"
    default="false"
    description="Change the underlying rendered element to a Slot component for polymorphism."
  />
</PropsTable>
